#include <iostream>
using namespace std;

//linked list

struct Node{
  int key;
  struct Node* next;
};

int main()
{
  /*Simple linked list insertion manually*/
  // struct Node* head;
  // struct Node* second;
  // struct Node* third;
  // head=(struct Node*)malloc(sizeof(struct Node));
  // second=(struct Node*)malloc(sizeof(struct Node));
  // third=(struct Node*)malloc(sizeof(struct Node));
  //
  // head->key=1;
  // head->next=second;
  // second->key=2;
  // second->next=third;
  // third->key=3;
  // third->next=NULL;
  // std::cout << head->key << '\n';
  // std::cout << head->next->next->key << '\n';
}
