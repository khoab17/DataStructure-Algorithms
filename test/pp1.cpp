#include<bits/stdc++.h>
using namespace std;

typedef long long ll;

int main()
{
    int t_case;
    cin>>t_case;

    while(t_case--)
    {
        int a,b,c,d;
        cin>>a>>b>>c>>d;

        int flag=0;
        while(1)
        {
            if( (a/b) == (a/b) )
                break;
            if( (a/b) > (c/d) )
            {
                c=a+c;
            }
            else{

            }
        }
        cout<<falg<<"\n";
    }

}