#include<bits/stdc++.h>
using namespace std;

void solve(vector<int> &arr,int n){
        
}

int main()
{
    int t_case;
    cin>>t_case;

    while (t_case--)
    {
        int n;
        cin>>n;
        vector<int> arr(n);
        for(int i=0; i<n; i++)
            cin>>arr[i];
        
        solve(arr,n);
    }
    
}